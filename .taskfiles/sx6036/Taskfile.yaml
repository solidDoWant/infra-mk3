---
version: 3

vars:
  SX6036_MGMT_IP: 10.0.0.2

includes:
  local: local.yaml
  routine:
    taskfile: routine.yaml
    flatten: true
  ssh:
    taskfile: ssh.yaml
    flatten: true
  setup: setup.yaml
