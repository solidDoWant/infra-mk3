---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Component-only (mirrors the namespace issuers): supplies just the
# per-namespace FluentBitConfig enabler. This namespace's log filters live in
# ../filters (applied by a separate Flux Kustomization).
components:
  - ../../../../components/logging/namespace
