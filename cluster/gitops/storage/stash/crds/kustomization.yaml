---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./hr.yaml
  # For some dumb reason this isn't included in the CRDs for stash, but it needs it for some resources
  - https://github.com/kmodules/custom-resources//crds/appcatalog.appscode.com_appbindings.yaml?timeout=120&version=v0.30.1
