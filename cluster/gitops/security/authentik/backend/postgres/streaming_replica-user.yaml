---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: postgres-streaming_replica-user
spec:
  secretName: postgres-streaming_replica-user
  usages:
    - client auth
  commonName: streaming_replica
  duration: 1h
  secretTemplate:
    labels:
      cnpg.io/reload: "true"
  issuerRef:
    group: cert-manager.io
    name: postgres-auth-ca
    kind: Issuer
---
# Approve the the internal-gateway-istio certificate specifically.
apiVersion: policy.cert-manager.io/v1alpha1
kind: CertificateRequestPolicy
metadata:
  name: postgres-auth-ca-postgres-streaming_replica-user
spec:
  selector:
    issuerRef:
      group: cert-manager.io
      kind: Issuer
      name: postgres-auth-ca
    namespace:
      matchNames:
        - security
  allowed:
    commonName:
      required: true
      value: streaming_replica
    usages:
      - client auth
  constraints:
    maxDuration: 1h
