---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./cluster-serving-cert.yaml
  - ./cluster.yaml
configMapGenerator:
  - name: teleport-audit-db-registration
    files:
      - teleport-audit-db-registration.yaml
    options:
      disableNameSuffixHash: true
