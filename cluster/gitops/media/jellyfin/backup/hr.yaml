---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: jellyfin-backup
spec:
  interval: 5m
  chart:
    spec:
      chart: dr-job
      reconcileStrategy: ChartVersion
      sourceRef:
        kind: HelmRepository
        namespace: flux-system
        name: soliddowant-charts
      version: 0.0.18
  values:
    jobConfig:
      drType: generic
      drAction: backup
      cronjob:
        schedule: "@daily"
      configFile:
        backupName: jellyfin-backup
        backupVolume:
          storageClass: zfs-generic-nfs # Backed by storage server
          size: 1Gi
        files:
          # Contains jellyfin's database, plugins, etc
          - name: config
            pvc: jellyfin
            snapshotClass: ssd-replicated-3x-block
            exclude:
              - glob: data/attachments
              - glob: data/subtitles
              - glob: log
              - glob: lost+found
              - glob: metadata
              - glob: temp
