---
# Sets the HelmRelease values config map name to the one generated by the
# kustomization. When the values update, the config map name will change,
# triggering a HelmRelease reconciliation.
nameReference:
  - kind: ConfigMap
    fieldSpecs:
      - path: spec/valuesFrom/name
        kind: HelmRelease
