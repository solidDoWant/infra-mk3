---
apiVersion: policy.cert-manager.io/v1alpha1
kind: CertificateRequestPolicy
metadata:
  name: data-intermediary-ca-temporal-internal-frontend
spec:
  selector:
    issuerRef:
      group: cert-manager.io
      kind: Issuer
      name: data-intermediary-ca
    namespace:
      matchNames:
        - data
  allowed:
    dnsNames:
      required: true
      values:
        - temporal-internal-frontend.data.svc
        - temporal-internal-frontend.data.svc.cluster.local
    subject:
      countries:
        required: true
        values:
          - US
      provinces:
        required: true
        values:
          - ${SECRET_STATE}
      organizations:
        required: true
        values:
          - infra-mk3
    usages:
      - server auth
  constraints:
    maxDuration: 4h
