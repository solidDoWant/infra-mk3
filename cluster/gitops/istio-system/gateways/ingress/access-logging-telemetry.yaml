---
apiVersion: telemetry.istio.io/v1
kind: Telemetry
metadata:
  name: ingress-gateways
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: ingress-gateways
  accessLogging:
    - providers:
        - name: envoy
