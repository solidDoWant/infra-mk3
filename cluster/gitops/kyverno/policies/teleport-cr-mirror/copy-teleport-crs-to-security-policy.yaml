# DO NOT EDIT — generated by gen-policies.sh in this directory.
# Re-run the script to refresh the kinds list against the cluster.
---
# yaml-language-server: $schema=https://raw.githubusercontent.com/datreeio/CRDs-catalog/main/kyverno.io/clusterpolicy_v1.json
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: copy-teleport-crs-to-security-namespace
spec:
  # Process pre-existing CRs on policy install/update so they are mirrored
  # without requiring a re-apply of every source.
  generateExisting: true
  rules:
    - name: copy-teleport-cr-to-security
      match:
        any:
          - resources:
              kinds:
                - resources.teleport.dev/v1/TeleportAccessList
                - resources.teleport.dev/v1/TeleportAccessMonitoringRuleV1
                - resources.teleport.dev/v1/TeleportAppV3
                - resources.teleport.dev/v1/TeleportAutoupdateConfigV1
                - resources.teleport.dev/v1/TeleportAutoupdateVersionV1
                - resources.teleport.dev/v1/TeleportBotV1
                - resources.teleport.dev/v1/TeleportDatabaseV3
                - resources.teleport.dev/v1/TeleportInferenceModel
                - resources.teleport.dev/v1/TeleportInferencePolicy
                - resources.teleport.dev/v1/TeleportInferenceSecret
                - resources.teleport.dev/v1/TeleportLockV2
                - resources.teleport.dev/v1/TeleportLoginRule
                - resources.teleport.dev/v1/TeleportOktaImportRule
                - resources.teleport.dev/v1/TeleportOpenSSHEICEServerV2
                - resources.teleport.dev/v1/TeleportOpenSSHServerV2
                - resources.teleport.dev/v1/TeleportRetrievalModelV1
                - resources.teleport.dev/v1/TeleportRoleV6
                - resources.teleport.dev/v1/TeleportRoleV7
                - resources.teleport.dev/v1/TeleportRoleV8
                - resources.teleport.dev/v1/TeleportSAMLIdPServiceProviderV1
                - resources.teleport.dev/v1/TeleportScopedRoleAssignmentV1
                - resources.teleport.dev/v1/TeleportScopedRoleV1
                - resources.teleport.dev/v1/TeleportScopedTokenV1
                - resources.teleport.dev/v1/TeleportTrustedClusterV2
                - resources.teleport.dev/v1/TeleportWorkloadIdentityV1
                - resources.teleport.dev/v2/TeleportProvisionToken
                - resources.teleport.dev/v2/TeleportSAMLConnector
                - resources.teleport.dev/v2/TeleportUser
                - resources.teleport.dev/v3/TeleportGithubConnector
                - resources.teleport.dev/v3/TeleportOIDCConnector
                - resources.teleport.dev/v5/TeleportRole
      preconditions:
        all:
          - key: "{{ request.namespace }}"
            operator: NotEquals
            value: security
      generate:
        # apiVersion/kind/name templated from the trigger so a single rule
        # covers every kind in the match list. Cross-namespace name
        # collisions are rejected by Kyverno (loud failure) — service-scoped
        # source names should make this rare in practice.
        apiVersion: "{{ request.object.apiVersion }}"
        kind: "{{ request.object.kind }}"
        name: "{{ request.object.metadata.name }}"
        namespace: security
        synchronize: true
        clone:
          namespace: "{{ request.namespace }}"
          name: "{{ request.object.metadata.name }}"
