# DO NOT EDIT — generated by gen-policies.sh in this directory.
# Edit the KINDS list there and re-run the script to update.
---
# yaml-language-server: $schema=https://raw.githubusercontent.com/datreeio/CRDs-catalog/main/policies.kyverno.io/generatingpolicy_v1.json
apiVersion: policies.kyverno.io/v1
kind: GeneratingPolicy
metadata:
  name: copy-teleportaccesslists-to-security
spec:
  matchConstraints:
    resourceRules:
      - apiGroups: [resources.teleport.dev]
        apiVersions: ["*"]
        operations: ["*"]
        resources: [teleportaccesslists]
  matchConditions:
    - name: only-create-or-update
      expression: request.operation in ['CREATE', 'UPDATE']
    - name: not-already-in-security
      expression: object.metadata.namespace != 'security'
  evaluation:
    synchronize:
      enabled: true
    generateExisting:
      enabled: true
  generate:
    - expression: generator.Apply('security', [object])
---
# yaml-language-server: $schema=https://raw.githubusercontent.com/datreeio/CRDs-catalog/main/policies.kyverno.io/generatingpolicy_v1.json
apiVersion: policies.kyverno.io/v1
kind: GeneratingPolicy
metadata:
  name: copy-teleportappsv3-to-security
spec:
  matchConstraints:
    resourceRules:
      - apiGroups: [resources.teleport.dev]
        apiVersions: ["*"]
        operations: ["*"]
        resources: [teleportappsv3]
  matchConditions:
    - name: only-create-or-update
      expression: request.operation in ['CREATE', 'UPDATE']
    - name: not-already-in-security
      expression: object.metadata.namespace != 'security'
  evaluation:
    synchronize:
      enabled: true
    generateExisting:
      enabled: true
  generate:
    - expression: generator.Apply('security', [object])
---
# yaml-language-server: $schema=https://raw.githubusercontent.com/datreeio/CRDs-catalog/main/policies.kyverno.io/generatingpolicy_v1.json
apiVersion: policies.kyverno.io/v1
kind: GeneratingPolicy
metadata:
  name: copy-teleportautoupdateconfigsv1-to-security
spec:
  matchConstraints:
    resourceRules:
      - apiGroups: [resources.teleport.dev]
        apiVersions: ["*"]
        operations: ["*"]
        resources: [teleportautoupdateconfigsv1]
  matchConditions:
    - name: only-create-or-update
      expression: request.operation in ['CREATE', 'UPDATE']
    - name: not-already-in-security
      expression: object.metadata.namespace != 'security'
  evaluation:
    synchronize:
      enabled: true
    generateExisting:
      enabled: true
  generate:
    - expression: generator.Apply('security', [object])
---
# yaml-language-server: $schema=https://raw.githubusercontent.com/datreeio/CRDs-catalog/main/policies.kyverno.io/generatingpolicy_v1.json
apiVersion: policies.kyverno.io/v1
kind: GeneratingPolicy
metadata:
  name: copy-teleportautoupdateversionsv1-to-security
spec:
  matchConstraints:
    resourceRules:
      - apiGroups: [resources.teleport.dev]
        apiVersions: ["*"]
        operations: ["*"]
        resources: [teleportautoupdateversionsv1]
  matchConditions:
    - name: only-create-or-update
      expression: request.operation in ['CREATE', 'UPDATE']
    - name: not-already-in-security
      expression: object.metadata.namespace != 'security'
  evaluation:
    synchronize:
      enabled: true
    generateExisting:
      enabled: true
  generate:
    - expression: generator.Apply('security', [object])
---
# yaml-language-server: $schema=https://raw.githubusercontent.com/datreeio/CRDs-catalog/main/policies.kyverno.io/generatingpolicy_v1.json
apiVersion: policies.kyverno.io/v1
kind: GeneratingPolicy
metadata:
  name: copy-teleportbotsv1-to-security
spec:
  matchConstraints:
    resourceRules:
      - apiGroups: [resources.teleport.dev]
        apiVersions: ["*"]
        operations: ["*"]
        resources: [teleportbotsv1]
  matchConditions:
    - name: only-create-or-update
      expression: request.operation in ['CREATE', 'UPDATE']
    - name: not-already-in-security
      expression: object.metadata.namespace != 'security'
  evaluation:
    synchronize:
      enabled: true
    generateExisting:
      enabled: true
  generate:
    - expression: generator.Apply('security', [object])
---
# yaml-language-server: $schema=https://raw.githubusercontent.com/datreeio/CRDs-catalog/main/policies.kyverno.io/generatingpolicy_v1.json
apiVersion: policies.kyverno.io/v1
kind: GeneratingPolicy
metadata:
  name: copy-teleportgithubconnectors-to-security
spec:
  matchConstraints:
    resourceRules:
      - apiGroups: [resources.teleport.dev]
        apiVersions: ["*"]
        operations: ["*"]
        resources: [teleportgithubconnectors]
  matchConditions:
    - name: only-create-or-update
      expression: request.operation in ['CREATE', 'UPDATE']
    - name: not-already-in-security
      expression: object.metadata.namespace != 'security'
  evaluation:
    synchronize:
      enabled: true
    generateExisting:
      enabled: true
  generate:
    - expression: generator.Apply('security', [object])
---
# yaml-language-server: $schema=https://raw.githubusercontent.com/datreeio/CRDs-catalog/main/policies.kyverno.io/generatingpolicy_v1.json
apiVersion: policies.kyverno.io/v1
kind: GeneratingPolicy
metadata:
  name: copy-teleportloginrules-to-security
spec:
  matchConstraints:
    resourceRules:
      - apiGroups: [resources.teleport.dev]
        apiVersions: ["*"]
        operations: ["*"]
        resources: [teleportloginrules]
  matchConditions:
    - name: only-create-or-update
      expression: request.operation in ['CREATE', 'UPDATE']
    - name: not-already-in-security
      expression: object.metadata.namespace != 'security'
  evaluation:
    synchronize:
      enabled: true
    generateExisting:
      enabled: true
  generate:
    - expression: generator.Apply('security', [object])
---
# yaml-language-server: $schema=https://raw.githubusercontent.com/datreeio/CRDs-catalog/main/policies.kyverno.io/generatingpolicy_v1.json
apiVersion: policies.kyverno.io/v1
kind: GeneratingPolicy
metadata:
  name: copy-teleportoidcconnectors-to-security
spec:
  matchConstraints:
    resourceRules:
      - apiGroups: [resources.teleport.dev]
        apiVersions: ["*"]
        operations: ["*"]
        resources: [teleportoidcconnectors]
  matchConditions:
    - name: only-create-or-update
      expression: request.operation in ['CREATE', 'UPDATE']
    - name: not-already-in-security
      expression: object.metadata.namespace != 'security'
  evaluation:
    synchronize:
      enabled: true
    generateExisting:
      enabled: true
  generate:
    - expression: generator.Apply('security', [object])
---
# yaml-language-server: $schema=https://raw.githubusercontent.com/datreeio/CRDs-catalog/main/policies.kyverno.io/generatingpolicy_v1.json
apiVersion: policies.kyverno.io/v1
kind: GeneratingPolicy
metadata:
  name: copy-teleportoktaimportrules-to-security
spec:
  matchConstraints:
    resourceRules:
      - apiGroups: [resources.teleport.dev]
        apiVersions: ["*"]
        operations: ["*"]
        resources: [teleportoktaimportrules]
  matchConditions:
    - name: only-create-or-update
      expression: request.operation in ['CREATE', 'UPDATE']
    - name: not-already-in-security
      expression: object.metadata.namespace != 'security'
  evaluation:
    synchronize:
      enabled: true
    generateExisting:
      enabled: true
  generate:
    - expression: generator.Apply('security', [object])
---
# yaml-language-server: $schema=https://raw.githubusercontent.com/datreeio/CRDs-catalog/main/policies.kyverno.io/generatingpolicy_v1.json
apiVersion: policies.kyverno.io/v1
kind: GeneratingPolicy
metadata:
  name: copy-teleportopenssheiceserversv2-to-security
spec:
  matchConstraints:
    resourceRules:
      - apiGroups: [resources.teleport.dev]
        apiVersions: ["*"]
        operations: ["*"]
        resources: [teleportopenssheiceserversv2]
  matchConditions:
    - name: only-create-or-update
      expression: request.operation in ['CREATE', 'UPDATE']
    - name: not-already-in-security
      expression: object.metadata.namespace != 'security'
  evaluation:
    synchronize:
      enabled: true
    generateExisting:
      enabled: true
  generate:
    - expression: generator.Apply('security', [object])
---
# yaml-language-server: $schema=https://raw.githubusercontent.com/datreeio/CRDs-catalog/main/policies.kyverno.io/generatingpolicy_v1.json
apiVersion: policies.kyverno.io/v1
kind: GeneratingPolicy
metadata:
  name: copy-teleportopensshserversv2-to-security
spec:
  matchConstraints:
    resourceRules:
      - apiGroups: [resources.teleport.dev]
        apiVersions: ["*"]
        operations: ["*"]
        resources: [teleportopensshserversv2]
  matchConditions:
    - name: only-create-or-update
      expression: request.operation in ['CREATE', 'UPDATE']
    - name: not-already-in-security
      expression: object.metadata.namespace != 'security'
  evaluation:
    synchronize:
      enabled: true
    generateExisting:
      enabled: true
  generate:
    - expression: generator.Apply('security', [object])
---
# yaml-language-server: $schema=https://raw.githubusercontent.com/datreeio/CRDs-catalog/main/policies.kyverno.io/generatingpolicy_v1.json
apiVersion: policies.kyverno.io/v1
kind: GeneratingPolicy
metadata:
  name: copy-teleportprovisiontokens-to-security
spec:
  matchConstraints:
    resourceRules:
      - apiGroups: [resources.teleport.dev]
        apiVersions: ["*"]
        operations: ["*"]
        resources: [teleportprovisiontokens]
  matchConditions:
    - name: only-create-or-update
      expression: request.operation in ['CREATE', 'UPDATE']
    - name: not-already-in-security
      expression: object.metadata.namespace != 'security'
  evaluation:
    synchronize:
      enabled: true
    generateExisting:
      enabled: true
  generate:
    - expression: generator.Apply('security', [object])
---
# yaml-language-server: $schema=https://raw.githubusercontent.com/datreeio/CRDs-catalog/main/policies.kyverno.io/generatingpolicy_v1.json
apiVersion: policies.kyverno.io/v1
kind: GeneratingPolicy
metadata:
  name: copy-teleportroles-to-security
spec:
  matchConstraints:
    resourceRules:
      - apiGroups: [resources.teleport.dev]
        apiVersions: ["*"]
        operations: ["*"]
        resources: [teleportroles]
  matchConditions:
    - name: only-create-or-update
      expression: request.operation in ['CREATE', 'UPDATE']
    - name: not-already-in-security
      expression: object.metadata.namespace != 'security'
  evaluation:
    synchronize:
      enabled: true
    generateExisting:
      enabled: true
  generate:
    - expression: generator.Apply('security', [object])
---
# yaml-language-server: $schema=https://raw.githubusercontent.com/datreeio/CRDs-catalog/main/policies.kyverno.io/generatingpolicy_v1.json
apiVersion: policies.kyverno.io/v1
kind: GeneratingPolicy
metadata:
  name: copy-teleportrolesv6-to-security
spec:
  matchConstraints:
    resourceRules:
      - apiGroups: [resources.teleport.dev]
        apiVersions: ["*"]
        operations: ["*"]
        resources: [teleportrolesv6]
  matchConditions:
    - name: only-create-or-update
      expression: request.operation in ['CREATE', 'UPDATE']
    - name: not-already-in-security
      expression: object.metadata.namespace != 'security'
  evaluation:
    synchronize:
      enabled: true
    generateExisting:
      enabled: true
  generate:
    - expression: generator.Apply('security', [object])
---
# yaml-language-server: $schema=https://raw.githubusercontent.com/datreeio/CRDs-catalog/main/policies.kyverno.io/generatingpolicy_v1.json
apiVersion: policies.kyverno.io/v1
kind: GeneratingPolicy
metadata:
  name: copy-teleportrolesv7-to-security
spec:
  matchConstraints:
    resourceRules:
      - apiGroups: [resources.teleport.dev]
        apiVersions: ["*"]
        operations: ["*"]
        resources: [teleportrolesv7]
  matchConditions:
    - name: only-create-or-update
      expression: request.operation in ['CREATE', 'UPDATE']
    - name: not-already-in-security
      expression: object.metadata.namespace != 'security'
  evaluation:
    synchronize:
      enabled: true
    generateExisting:
      enabled: true
  generate:
    - expression: generator.Apply('security', [object])
---
# yaml-language-server: $schema=https://raw.githubusercontent.com/datreeio/CRDs-catalog/main/policies.kyverno.io/generatingpolicy_v1.json
apiVersion: policies.kyverno.io/v1
kind: GeneratingPolicy
metadata:
  name: copy-teleportrolesv8-to-security
spec:
  matchConstraints:
    resourceRules:
      - apiGroups: [resources.teleport.dev]
        apiVersions: ["*"]
        operations: ["*"]
        resources: [teleportrolesv8]
  matchConditions:
    - name: only-create-or-update
      expression: request.operation in ['CREATE', 'UPDATE']
    - name: not-already-in-security
      expression: object.metadata.namespace != 'security'
  evaluation:
    synchronize:
      enabled: true
    generateExisting:
      enabled: true
  generate:
    - expression: generator.Apply('security', [object])
---
# yaml-language-server: $schema=https://raw.githubusercontent.com/datreeio/CRDs-catalog/main/policies.kyverno.io/generatingpolicy_v1.json
apiVersion: policies.kyverno.io/v1
kind: GeneratingPolicy
metadata:
  name: copy-teleportsamlconnectors-to-security
spec:
  matchConstraints:
    resourceRules:
      - apiGroups: [resources.teleport.dev]
        apiVersions: ["*"]
        operations: ["*"]
        resources: [teleportsamlconnectors]
  matchConditions:
    - name: only-create-or-update
      expression: request.operation in ['CREATE', 'UPDATE']
    - name: not-already-in-security
      expression: object.metadata.namespace != 'security'
  evaluation:
    synchronize:
      enabled: true
    generateExisting:
      enabled: true
  generate:
    - expression: generator.Apply('security', [object])
---
# yaml-language-server: $schema=https://raw.githubusercontent.com/datreeio/CRDs-catalog/main/policies.kyverno.io/generatingpolicy_v1.json
apiVersion: policies.kyverno.io/v1
kind: GeneratingPolicy
metadata:
  name: copy-teleporttrustedclustersv2-to-security
spec:
  matchConstraints:
    resourceRules:
      - apiGroups: [resources.teleport.dev]
        apiVersions: ["*"]
        operations: ["*"]
        resources: [teleporttrustedclustersv2]
  matchConditions:
    - name: only-create-or-update
      expression: request.operation in ['CREATE', 'UPDATE']
    - name: not-already-in-security
      expression: object.metadata.namespace != 'security'
  evaluation:
    synchronize:
      enabled: true
    generateExisting:
      enabled: true
  generate:
    - expression: generator.Apply('security', [object])
---
# yaml-language-server: $schema=https://raw.githubusercontent.com/datreeio/CRDs-catalog/main/policies.kyverno.io/generatingpolicy_v1.json
apiVersion: policies.kyverno.io/v1
kind: GeneratingPolicy
metadata:
  name: copy-teleportusers-to-security
spec:
  matchConstraints:
    resourceRules:
      - apiGroups: [resources.teleport.dev]
        apiVersions: ["*"]
        operations: ["*"]
        resources: [teleportusers]
  matchConditions:
    - name: only-create-or-update
      expression: request.operation in ['CREATE', 'UPDATE']
    - name: not-already-in-security
      expression: object.metadata.namespace != 'security'
  evaluation:
    synchronize:
      enabled: true
    generateExisting:
      enabled: true
  generate:
    - expression: generator.Apply('security', [object])
---
# yaml-language-server: $schema=https://raw.githubusercontent.com/datreeio/CRDs-catalog/main/policies.kyverno.io/generatingpolicy_v1.json
apiVersion: policies.kyverno.io/v1
kind: GeneratingPolicy
metadata:
  name: copy-teleportworkloadidentitiesv1-to-security
spec:
  matchConstraints:
    resourceRules:
      - apiGroups: [resources.teleport.dev]
        apiVersions: ["*"]
        operations: ["*"]
        resources: [teleportworkloadidentitiesv1]
  matchConditions:
    - name: only-create-or-update
      expression: request.operation in ['CREATE', 'UPDATE']
    - name: not-already-in-security
      expression: object.metadata.namespace != 'security'
  evaluation:
    synchronize:
      enabled: true
    generateExisting:
      enabled: true
  generate:
    - expression: generator.Apply('security', [object])
