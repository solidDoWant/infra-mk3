# Per-Dependency Version Policies

This file is the registry of versioning policies the `plan-upgrades` skill consults when computing latest-stable targets and upgrade paths. The skill appends new entries here as it encounters new dependencies.

## Schema

Each entry is a level-2 heading naming the dep, followed by these fields:

- **Identifies**: how to recognize this dep in the cluster (chart name(s), image repo(s), both).
- **Stable definition**: the rule for what counts as a stable release. Free-form English — clear enough that a researching subagent can apply it to a list of available versions and pick the latest stable.
- **Release notes**: URL or URL pattern where release notes live. Include placeholder `{version}` if the URL is per-release.
- **Breaking-change indicators**: where to look for breaking changes (a section heading in release notes, a separate `BREAKING-CHANGES.md`, the upgrade guide URL, etc.).
- **Skip-allowed**: whether minor (or major) versions can be skipped, and any constraints on doing so.

When the skill encounters a dep that does not have an entry here, it must ask the user for these fields and append a new entry. Never silently guess a policy.

## Entries

### ceph

- **Identifies**: image `quay.io/ceph/ceph`; also referenced via `rook-ceph-cluster` HelmRelease values and CephCluster CRD `spec.cephVersion.image`.
- **Stable definition**: `<major>.<minor>.<patch>` where `<minor> >= 2`. e.g. `18.2.0` is stable, `18.1.x` is not. The first stable of a major is `<major>.2.0`.
- **Release notes**: https://docs.ceph.com/en/latest/releases/
- **Breaking-change indicators**: each release page has an "Upgrading from previous releases" section; cross-check with the rook-ceph upgrade guide at https://rook.io/docs/rook/latest/Upgrade/ceph-upgrade/ since Rook gates supported Ceph versions.
- **Skip-allowed**: minor versions can be skipped within the same major. Major upgrades must traverse one major at a time per Ceph upgrade docs.

<!-- New entries are appended below this line. Format follows the schema above. -->

### authentik

- **Identifies**: Helm chart `authentik` from HelmRepository `goauthentik-charts` (https://charts.goauthentik.io); server image `ghcr.io/goauthentik/server`; proxy/outpost image `ghcr.io/goauthentik/proxy`. The chart `version`, the server image `tag`, and the proxy image `tag` all move in lockstep to the same CalVer string (e.g. `2026.5.2`) — bump all three together.
- **Stable definition**: a CalVer tag `YYYY.M.patch` (e.g. `2025.8.3`, `2026.5.2`) published as a non-prerelease GitHub release at https://github.com/goauthentik/authentik/releases AND present as a non-prerelease chart version in https://charts.goauthentik.io/index.yaml. Reject anything with `-rc`, `-beta`, `-alpha` suffixes. The latest stable is the highest such CalVer (compare year, then month, then patch numerically).
- **Release notes**: per-minor release notes at https://docs.goauthentik.io/docs/releases/{YYYY.M} (e.g. https://docs.goauthentik.io/docs/releases/2026.5); per-tag GitHub release at https://github.com/goauthentik/authentik/releases/tag/version/{version}; upgrade/installation docs at https://docs.goauthentik.io/docs/install-config/upgrade.
- **Breaking-change indicators**: each per-minor release-notes page has "Breaking changes" and "API changes" / "Removed/Deprecated" sections, plus a "How to upgrade" note. Watch for: renamed/removed `AUTHENTIK_*` env vars (this cluster configures authentik entirely via env in `app/hr.yaml`), blueprint schema / `!Tag` behavior changes (cluster uses blueprints heavily via the k8s-sidecar), outpost API/protocol changes (the `ghcr.io/goauthentik/proxy` outpost must stay version-matched to the server), and minimum postgres/redis version bumps. Cross-check the chart `values.yaml` diff between from/to versions for renamed keys.
- **Skip-allowed**: **No minor skipping.** authentik's migration model runs DB migrations forward across each minor; the project documents upgrading one release (minor) at a time and only supports skipping when explicitly stated. Traverse minors sequentially (`2025.8 → 2025.10 → 2025.12 → 2026.2 → ...` — confirm the actual published minors), landing on the latest patch of each intermediate minor. Patch versions within a minor may be skipped.

### istio

- **Identifies**: Helm charts `base`, `cni`, `istiod`, `ztunnel` from `https://istio-release.storage.googleapis.com/charts` (HelmRepository `istio-charts`). Chart `appVersion` selects the matching `docker.io/istio/*` images (`pilot`, `proxyv2`, `install-cni`, `ztunnel`) — they are not pinned independently in this cluster.
- **Stable definition**: a tag `X.Y.Z` published as a non-prerelease release at https://github.com/istio/istio/releases and present in the istio-charts `index.yaml`. Reject anything with `-rc`, `-beta`, `-alpha`, `-dev`, or `-distroless` suffixes (distroless is a separate image variant, not a version). The latest stable is the highest such tag still inside Istio's [supported releases window](https://istio.io/latest/docs/releases/supported-releases/) (typically N and N-1 minors).
- **Release notes**: per-release announcement at https://istio.io/latest/news/releases/{major}.{minor}.x/announcing-{major}.{minor}/ ; per-patch change notes at https://istio.io/latest/news/releases/{major}.{minor}.x/announcing-{major}.{minor}.{patch}/ ; per-minor upgrade notes at https://istio.io/latest/news/releases/{major}.{minor}.x/announcing-{major}.{minor}/upgrade-notes/ ; GitHub release page at https://github.com/istio/istio/releases/tag/{version}
- **Breaking-change indicators**: each minor announcement has a dedicated "Upgrade Notes" page with "Breaking Changes" and "Action Required" sections. Cross-check the per-minor "What's New" page, the per-patch announcement pages, and any open issues at https://github.com/istio/istio/issues with the `area/ambient`, `area/networking`, or `area/security` labels that mention the target version range. The four charts (`base`, `cni`, `istiod`, `ztunnel`) ship in lockstep — always bump them together to the same `X.Y.Z`.
- **Skip-allowed**: **No minor skipping.** Per https://istio.io/latest/docs/setup/upgrade/ Istio is only tested for upgrades between adjacent minors (`1.26 → 1.27 → 1.28`). Patch versions within a minor may be skipped — when traversing minors, land on the latest patch of each intermediate minor before bumping to the next minor. Ambient-mode upgrades have additional ordering constraints (CNI → ztunnel → istiod → waypoints) that the per-minor upgrade notes call out.

### istio-csr

- **Identifies**: Helm chart `cert-manager-istio-csr` from `https://charts.jetstack.io` (HelmRepository `jetstack-charts`); image `quay.io/jetstack/cert-manager-istio-csr` (selected by chart `appVersion`).
- **Stable definition**: a tag `X.Y.Z` published as a non-prerelease GitHub release at https://github.com/cert-manager/istio-csr/releases AND a non-prerelease chart version at https://charts.jetstack.io/index.yaml. Reject anything with `-alpha`, `-beta`, `-rc` suffixes. Pre-1.0, so each `0.Y.0` minor is effectively a major release per semver — treat minor bumps as potentially breaking.
- **Release notes**: per-release at https://github.com/cert-manager/istio-csr/releases/tag/v{version} ; project README + docs at https://github.com/cert-manager/istio-csr ; cert-manager Istio integration guide at https://cert-manager.io/docs/usage/istio-csr/
- **Breaking-change indicators**: each GitHub release notes page has "Breaking changes" / "Notable changes" subsections. Cross-check the `Chart.yaml` `appVersion` bump against the istio-csr image's compatibility matrix (the project README lists supported Istio minors). Watch the chart's `values.yaml` diff between versions for renamed/removed values keys.
- **Skip-allowed**: **No minor skipping.** Pre-1.0 minor bumps may include breaking changes; traverse minors sequentially (`0.14 → 0.15 → 0.16 ...`). Patch versions within a minor may be skipped.

### cilium

- **Identifies**: Helm chart `cilium` from `https://helm.cilium.io/`; also the bootstrap helmfile in `cluster/bootstrap/helmfile.yaml` (chart version moves in lockstep with the HelmRelease). Cilium images (`quay.io/cilium/cilium`, `quay.io/cilium/operator-generic`, `quay.io/cilium/hubble-relay`, `quay.io/cilium/hubble-ui*`, `quay.io/cilium/cilium-envoy`) are not pinned separately — the chart's `appVersion` selects them.
- **Stable definition**: a tag `vX.Y.Z` published as a non-prerelease release at https://github.com/cilium/cilium/releases (i.e. `prerelease=false`) and that matches strict semver `MAJOR.MINOR.PATCH` with no suffix. Reject anything containing `-rc`, `-pre`, `-alpha`, `-beta`, etc. The latest stable is the highest such tag.
- **Release notes**: per-release at https://github.com/cilium/cilium/releases/tag/v{version} ; upgrade guide at https://docs.cilium.io/en/stable/operations/upgrade/ ; per-minor upgrade notes at https://docs.cilium.io/en/v{major}.{minor}/operations/upgrade/#current-release-required-changes
- **Breaking-change indicators**: each release notes page has "Major Changes" / "Minor Changes" / "Bugfixes" sections; the per-minor upgrade page has a "Current Release Required Changes" / "Upgrade Notes" subsection listing required values changes, deprecations, and removed flags. The "Annotations" / "Helm values" tables on the upgrade page surface renamed Helm keys.
- **Skip-allowed**: **No minor skipping.** The Cilium upgrade guide states the only tested rollback/upgrade path is between consecutive minor releases (`1.17 → 1.18 → 1.19`). Patch versions within a minor may be skipped — when traversing minors, land on the latest patch of each intermediate minor before bumping to the next minor.

### rook-ceph

- **Identifies**: Helm charts `rook-ceph` (operator) and `rook-ceph-cluster` (cluster/CRs) from `https://charts.rook.io/release`; also the operator image `rook/ceph` (referenced indirectly via the chart). The two charts are always version-locked.
- **Stable definition**: A Rook release `vX.Y.Z` is stable if **all** of: (a) it is published as a non-prerelease GitHub release at https://github.com/rook/rook/releases AND a non-prerelease chart version at https://charts.rook.io/release/index.yaml; (b) the chart's default `cephClusterSpec.cephVersion.image` resolves to a Ceph version that satisfies the **ceph** entry's stable definition above (i.e., Ceph `<major>.<minor>.<patch>` with `minor >= 2`). Reject any Rook version whose default Ceph image is not itself stable. Prefer the highest patch on the highest minor that meets both conditions.
- **Release notes**: per-release at https://github.com/rook/rook/releases/tag/v{version} ; upgrade guide at https://rook.io/docs/rook/latest/Upgrade/rook-upgrade/ ; Ceph upgrade compatibility at https://rook.io/docs/rook/latest/Upgrade/ceph-upgrade/
- **Breaking-change indicators**: each GitHub release notes page has a "Breaking Changes" section; the dedicated upgrade-guide page is the authoritative source for required ordering and pre-upgrade checks. Minimum supported Ceph version is stated per Rook minor in the release notes.
- **Skip-allowed**: **No minor skipping.** Rook supports upgrades between consecutive minors only (`1.16 → 1.17 → 1.18 → 1.19`). Patch versions within a minor may be skipped (e.g., `1.16.0 → 1.16.4` is fine). When traversing minors, take the latest patch of each intermediate minor as the landing version before moving to the next minor.

### kubernetes

- **Identifies**: Talos `kubernetesVersion` field in `talos/talconfig.yaml`. Also drives any kubectl image/binary pin (`registry.k8s.io/kubectl:vX.Y.Z`, `dl.k8s.io/release/vX.Y.Z/...`) — but those are tracked by the separate **kubectl** entry below.
- **Stable definition**: latest non-prerelease patch (`vX.Y.Z`, no `-rc`/`-alpha`/`-beta` suffix) on the highest minor that is **both** (a) in the Kubernetes "Supported Versions" window at https://kubernetes.io/releases/ AND (b) explicitly listed in the support matrix of the cluster's current Talos minor at https://docs.siderolabs.com/talos/v{talos_minor}/getting-started/support-matrix. Reject any minor not in both sets.
- **Release notes**: per-minor CHANGELOG at https://github.com/kubernetes/kubernetes/blob/release-{major}.{minor}/CHANGELOG/CHANGELOG-{major}.{minor}.md ; per-minor highlights at https://kubernetes.io/blog/ (search "Kubernetes v{major}.{minor}: ..."); deprecation guide at https://kubernetes.io/docs/reference/using-api/deprecation-guide/
- **Breaking-change indicators**: each minor's CHANGELOG has "Urgent Upgrade Notes (No, really, you MUST read this before you upgrade)" and "Deprecation" / "Removal" sections; cross-check the Talos release notes for the corresponding Talos minor for any host-side feature-gate or kubelet-flag changes; verify every feature gate set in `talconfig.yaml` (currently `DynamicResourceAllocation`, `DRAAdminAccess`, `DRAPartitionableDevices`, `UserNamespacesPodSecurityStandards`, `ImageVolume`) against the target minor's KEP graduation table — gates that have **graduated to GA and been removed** must be deleted from talconfig, gates that are removed-and-not-graduated must be deleted too. Also check the API removal list against any CRDs / built-in resources the cluster uses.
- **Skip-allowed**: **No minor skipping.** Kubernetes supports control-plane upgrades only between consecutive minors (`n → n+1`); kubelet skew tolerated is `n-3..n`. Patches within a minor may be skipped (always land on the latest patch of each minor when traversing).

### talos

- **Identifies**: Talos `talosVersion` field in `talos/talconfig.yaml`; installer images `ghcr.io/siderolabs/installer:vX.Y.Z`; image-factory URLs of the form `factory.talos.dev/{...}/vX.Y.Z`.
- **Stable definition**: latest non-prerelease patch (`vX.Y.Z`, no `-alpha`/`-beta`/`-rc` suffix) on the **specified minor** whose release notes confirm support for the target Kubernetes minor in the support matrix. Reject any patch published before the target Kubernetes minor's GA date (such patches predate the support claim).
- **Release notes**: per-release at https://github.com/siderolabs/talos/releases/tag/v{version} ; per-minor upgrade guide at https://www.talos.dev/v{major}.{minor}/talos-guides/upgrading-talos/ ; support matrix at https://docs.siderolabs.com/talos/v{major}.{minor}/getting-started/support-matrix
- **Breaking-change indicators**: each release page has a "Component Updates" and (when present) "Breaking Changes" / "Upgrade Notes" section; the per-minor upgrade guide is authoritative for required pre-upgrade steps; the support matrix is authoritative for which Kubernetes minors a given Talos minor supports.
- **Skip-allowed**: patches within a minor may be skipped (always land on the latest patch). Minor skipping (e.g. `1.11 → 1.13`) is **not supported** — Talos must traverse minors sequentially. (Out of scope for plans that explicitly constrain Talos to its current minor.)

### snapshot-controller (Piraeus chart)

- **Identifies**: Helm chart `snapshot-controller` from HelmRepository `piraeus-charts` (https://piraeus.io/helm-charts/). The chart is a community packaging of the upstream `kubernetes-csi/external-snapshotter` snapshot-controller. The controller image (`registry.k8s.io/sig-storage/snapshot-controller`) is selected by the chart's `appVersion` and not pinned independently in this cluster. Note: the CRDs in `cluster/gitops/storage/volume-snapshot/crds/` are sourced separately from `github.com/kubernetes-csi/external-snapshotter//client/config/crd` and tracked independently.
- **Stable definition**: a chart version `X.Y.Z` published in the piraeus-charts `index.yaml` (https://piraeus.io/helm-charts/index.yaml) with no `-rc`, `-alpha`, `-beta`, `-pre`, or `-dev` suffix. Cross-check with non-prerelease GitHub releases at https://github.com/piraeusdatastore/helm-charts/releases. The latest stable is the highest such version.
- **Release notes**: chart release notes at https://github.com/piraeusdatastore/helm-charts/releases (search for `snapshot-controller-{version}` tags); commits affecting the chart at https://github.com/piraeusdatastore/helm-charts/commits/main/charts/snapshot-controller ; upstream controller release notes at https://github.com/kubernetes-csi/external-snapshotter/releases (matched by the chart's `appVersion`); upstream README at https://github.com/kubernetes-csi/external-snapshotter#readme
- **Breaking-change indicators**: GitHub release pages for the chart rarely flag breaking changes explicitly — diff `charts/snapshot-controller/values.yaml` and `charts/snapshot-controller/templates/` between the from/to tags for renamed values keys, removed knobs, or new required fields. Cross-check the upstream `external-snapshotter` release for the chart's target `appVersion` — its "Action Required" / "Urgent Upgrade Notes" sections call out controller-side behavior changes, new feature gates, and CRD version requirements. Verify the chart's bundled CRD requirement against the separately-managed `crds/kustomization.yaml` pin.
- **Skip-allowed**: minor (and major) versions may be skipped. The chart is a thin wrapper around upstream and chart-major bumps are infrequent; pick the target patch directly. (Upstream `external-snapshotter` does not guarantee skip support — verify the controller `appVersion` jump is supported per its release notes when crossing majors.)

### kubectl

- **Identifies**: Image refs `registry.k8s.io/kubectl:vX.Y.Z`; binary download URLs `https://dl.k8s.io/release/vX.Y.Z/bin/...` in scripts or container images that pull kubectl at startup.
- **Stable definition**: latest non-prerelease patch (`vX.Y.Z`) on the **same minor as the cluster's `kubernetesVersion`** in `talos/talconfig.yaml`. Patch may run ahead of or behind the server patch within the same minor. Cross-minor skew is limited to ±1 minor per upstream policy at https://kubernetes.io/releases/version-skew-policy/#kubectl ; prefer matching the cluster minor exactly.
- **Release notes**: bundled inside the Kubernetes per-minor CHANGELOG (search the file for `### kubectl` sections) at https://github.com/kubernetes/kubernetes/blob/release-{major}.{minor}/CHANGELOG/CHANGELOG-{major}.{minor}.md
- **Breaking-change indicators**: kubectl flag/output deprecations are called out in the CHANGELOG's "Deprecation" section under kubectl; also check `kubectl ... --help` diffs across versions when a flag is suspected of being renamed.
- **Skip-allowed**: patches may be skipped freely. The minor must match (or be one off from) the chosen cluster k8s minor — bump kubectl in lockstep with each k8s minor upgrade.

### vaultwarden

- **Identifies**: image `vaultwarden/server` (Docker Hub), used with the `-alpine` tag variant in this cluster (e.g. `1.33.2-alpine`). This is the unofficial Rust Bitwarden-compatible server by dani-garcia.
- **Stable definition**: the highest non-prerelease tag `X.Y.Z` published at https://github.com/dani-garcia/vaultwarden/releases (reject `-alpha`, `-beta`, `-rc`, `testing`, and any pre-release marker). Use the matching `X.Y.Z-alpine` image variant published on Docker Hub (https://hub.docker.com/r/vaultwarden/server/tags) — the cluster standardizes on the alpine variant.
- **Release notes**: per-release at https://github.com/dani-garcia/vaultwarden/releases/tag/{version} ; wiki for operational/breaking guidance at https://github.com/dani-garcia/vaultwarden/wiki
- **Breaking-change indicators**: GitHub release notes call out breaking changes inline (look for "breaking", env-var renames/removals, DB migration notes, admin-token format changes). Cross-check the wiki pages referenced from release notes. Watch for changed/removed environment variables (this cluster configures vaultwarden entirely via env vars in `app/hr.yaml`) and any new required settings.
- **Skip-allowed**: minor and patch versions may be skipped, but read every intervening release's notes for breaking changes and DB migrations (migrations are forward-only and run automatically on start). No formal "no-skip" constraint, but landing on the latest patch is preferred.

### vwmetrics

- **Identifies**: image `ghcr.io/tricked-dev/vwmetrics`, typically pinned by a git-short-SHA tag plus `@sha256:` digest (e.g. `71b7637@sha256:...`). A vaultwarden-specific Prometheus metrics exporter that connects to the vaultwarden postgres DB.
- **Stable definition**: prefer the highest non-prerelease tagged release at https://github.com/Tricked-dev/vwmetrics/releases or a semver image tag at https://github.com/Tricked-dev/vwmetrics/pkgs/container/vwmetrics if one exists. If the project publishes no semver releases, recommend the newest commit on the default branch, pinned as `<short-sha>@sha256:<digest>` to match the existing pin style.
- **Release notes**: GitHub releases at https://github.com/Tricked-dev/vwmetrics/releases (may be empty); otherwise the commit log at https://github.com/Tricked-dev/vwmetrics/commits/ and GHCR tag list at https://github.com/Tricked-dev/vwmetrics/pkgs/container/vwmetrics.
- **Breaking-change indicators**: no formal release notes expected — diff commits since the current pin for changed env vars (`DATABASE_URL` and any new config), changed metrics port (currently `3040`), changed `/metrics` path, or changed DB query/permission requirements. Verify the exporter still works against the cluster's postgres major.
- **Skip-allowed**: N/A (SHA-pinned rolling). Move directly to the chosen target SHA/tag; review the full commit diff since `71b7637`.

### openebs

- **Identifies**: Helm chart `openebs` from HelmRepository `openebs-charts` (https://openebs.github.io/openebs). The umbrella chart embeds `zfs-localpv` as an active sub-chart (the only enabled engine in this cluster); `localpv-provisioner`, `lvm`, and `mayastor` sub-charts are all disabled. The `zfs-localpv` sub-chart version is not pinned independently — it is selected by the umbrella chart version.
- **Stable definition**: a chart version `X.Y.Z` (strict semver) published as a non-prerelease in the openebs-charts `index.yaml` with no `-alpha`, `-beta`, `-rc`, `-pre`, or `-dev` suffix. Cross-check with a non-prerelease GitHub release at https://github.com/openebs/openebs/releases. The latest stable is the highest such version.
- **Release notes**: umbrella chart releases at https://github.com/openebs/openebs/releases/tag/{version}; zfs-localpv sub-chart releases at https://github.com/openebs/zfs-localpv/releases; CHANGELOG at https://github.com/openebs/openebs/blob/main/CHANGELOG.md.
- **Breaking-change indicators**: GitHub release notes for both the umbrella chart and the bundled zfs-localpv sub-chart. Watch for: renamed Helm values under `zfs-localpv.*` (the only active sub-chart in this cluster); changes to the `openebs-pre-upgrade-hook` Job behavior (see https://github.com/openebs/openebs/issues/3893 — this hook is bugged and must retain API egress in the netpol); new pods or containers added to the ZFS controller Deployment (the cluster uses a postRenderer to remove container index `2` — this must be verified on every bump); CRD schema changes to `ZFSVolume`, `ZFSSnapshot`, `ZFSNode` (openebs installs and manages these CRDs). Also verify the `openebs-crds` sub-chart CRD flags remain correctly disabled (the cluster manages volume-snapshot CRDs separately via `volume-snapshot-crds`).
- **Skip-allowed**: to be determined by subagent research. If the OpenEBS upgrade docs state sequential-minor requirements, record that constraint. Otherwise, skipping minor versions is acceptable with a full read of each skipped minor's release notes.

### fluxcd

- **Identifies**: installed from upstream **remote manifests**, not the community Helm chart: `https://github.com/fluxcd/flux2//manifests/install/?version={version}` referenced in `cluster/bootstrap/kustomization.yaml`. The matching CLI image `ghcr.io/fluxcd/flux-cli:{version}` is pinned in `.devcontainer/Dockerfile` and moves in lockstep with the manifest version. Controllers are the per-component images under `ghcr.io/fluxcd/{source,kustomize,helm,notification}-controller` selected by the manifest version (image-automation-controller and image-reflector-controller are deleted via patch — updates handled by Renovate). Consumed CRD API groups cluster-wide: `kustomize.toolkit.fluxcd.io`, `helm.toolkit.fluxcd.io`, `source.toolkit.fluxcd.io`, `notification.toolkit.fluxcd.io`.
- **Stable definition**: a tag `vX.Y.Z` published as a non-prerelease GitHub release at https://github.com/fluxcd/flux2/releases. Reject anything with `-rc`, `-beta`, `-alpha` suffixes. The latest stable is the highest such semver tag.
- **Release notes**: per-tag GitHub release at https://github.com/fluxcd/flux2/releases/tag/{version}; component controller release notes at https://github.com/fluxcd/{source,kustomize,helm,notification}-controller/releases; CRD/API and migration docs at https://fluxcd.io/flux/.
- **Breaking-change indicators**: each flux2 GitHub release notes page has a "Highlights"/"Breaking changes" section and links to the component controller releases. Watch for: **CRD apiVersion promotions/deprecations** (e.g. beta→GA group moves — the cluster consumes `helm.toolkit.fluxcd.io/v2`, `kustomize.toolkit.fluxcd.io/v1`, `source.toolkit.fluxcd.io/v1`, `notification.toolkit.fluxcd.io/v1`); **changes to upstream manifest structure** that break the JSON-patch op paths in `cluster/bootstrap/kustomization.yaml` (container index `0`, volume index `0`, args/resources paths targeting `kustomize-controller|helm-controller|source-controller` and `notification-controller`); **renamed/removed controller flags or feature gates** (the cluster sets `--concurrent=24`, `--requeue-dependency=5s`, `--feature-gates=OOMWatch=true`, `--oom-watch-memory-threshold`, `--oom-watch-interval`); changes to default resource requests/limits; minimum kubernetes version bumps. Cross-check the flux2 release "Upgrade" notes and the per-controller CHANGELOGs.
- **Skip-allowed**: minor versions can be skipped — flux supports jumping directly to a newer release. Before applying, run `flux check` and (recommended) `flux migrate`/`flux diff` workflows. The hard requirement is that all in-cluster custom resources already use API versions still served by the target release; deprecated API versions must be migrated before the controllers are upgraded.

### gateway-api

- **Identifies**: Gateway API CRDs installed from upstream **remote kustomize manifest**, not a Helm chart: `https://github.com/kubernetes-sigs/gateway-api/releases/download/v{version}/experimental-install.yaml` referenced in `cluster/gitops/networking/crds/gateway-api/kustomization.yaml`. The cluster uses the **experimental** channel (provides `TCPRoute`, `TLSRoute`, `GRPCRoute` extras beyond standard). Consumed API group cluster-wide: `gateway.networking.k8s.io` (`Gateway`, `GatewayClass`, `HTTPRoute`, `TCPRoute`, `ReferenceGrant`, etc.). Primary consumer is Istio (ambient/waypoint + gateways); also k8s-gateway, Cilium, and various app `httproute.yaml`/`tcproute.yaml` files.
- **Stable definition**: a tag `vX.Y.Z` published as a non-prerelease GitHub release at https://github.com/kubernetes-sigs/gateway-api/releases. Reject anything with `-rc`, `-beta`, `-alpha` suffixes. The latest stable is the highest such semver tag — **but** the chosen target must be a Gateway API version explicitly supported by the running Istio minor (cross-check Istio's Gateway API support docs); do not outrun what Istio supports.
- **Release notes**: per-tag GitHub release at https://github.com/kubernetes-sigs/gateway-api/releases/tag/v{version}; changelog at https://github.com/kubernetes-sigs/gateway-api/blob/main/CHANGELOG.md; version/channel concepts at https://gateway-api.sigs.k8s.io/concepts/versioning/; Istio's supported Gateway API version noted in https://istio.io/latest/docs/tasks/traffic-management/ingress/gateway-api/.
- **Breaking-change indicators**: each GitHub release page has "Major Themes"/"Breaking Changes"/"Deprecations"/"API Changes" sections. Watch for: **CRD apiVersion promotions** (e.g. `v1beta1`→`v1` graduations — verify all in-cluster routes use API versions still served); resources **moving between standard and experimental channels**; **removed/renamed fields** on `Gateway`/`HTTPRoute`/`TCPRoute`; webhook removal (recent releases dropped the validating webhook in favor of CEL). Confirm the target version is compatible with the cluster's Kubernetes version (CEL validation needs a recent k8s).
- **Skip-allowed**: yes — CRD bundles can be applied directly to a newer version. Constraints: (1) all in-cluster custom resources must use API versions still served by the target bundle; (2) the target must remain within what the running Istio minor supports; (3) stay on the **experimental** channel (cluster depends on `TCPRoute` for the minecraft router and Istio ambient features). Always re-apply the full `experimental-install.yaml` for the target tag.

### cert-manager

- **Identifies**: Helm chart `cert-manager` from `https://charts.jetstack.io` (HelmRepository `jetstack-charts`). The chart's `appVersion` selects all component images (`quay.io/jetstack/cert-manager-controller`, `-webhook`, `-cainjector`, `-acmesolver`, `-startupapicheck`) — they are **not** pinned independently in this cluster. CRDs are installed by the chart (`crds.enabled: true`, `crds.keep: true`). Consumed API group cluster-wide: `cert-manager.io` (`Certificate`, `ClusterIssuer`, `Issuer`, `CertificateRequest`), plus `acme.cert-manager.io`. Downstream consumers include istio-csr (signs the Istio mesh CA), approver-policy, trust-manager, and every app/ClusterIssuer that mints TLS.
- **Stable definition**: a tag `vX.Y.Z` published as a non-prerelease GitHub release at https://github.com/cert-manager/cert-manager/releases AND present as a non-prerelease chart version in https://charts.jetstack.io/index.yaml. Reject anything with `-alpha`, `-beta`, `-rc` suffixes. The latest stable is the highest such semver tag.
- **Release notes**: per-release at https://github.com/cert-manager/cert-manager/releases/tag/v{version} ; per-minor release notes at https://cert-manager.io/docs/releases/release-notes/release-notes-{major}.{minor}/ ; upgrade guide index at https://cert-manager.io/docs/installation/upgrading/ ; supported-releases / kubernetes compatibility matrix at https://cert-manager.io/docs/releases/.
- **Breaking-change indicators**: each per-minor release-notes page has "Breaking changes" / "Major themes" / "Action required" sections; the per-minor upgrade pages under https://cert-manager.io/docs/installation/upgrading/ are authoritative for required ordering and pre-upgrade steps. Watch for: renamed/removed Helm values keys (this cluster sets many — `featureGates`, `disableAutoApproval`, `enableCertificateOwnerRef`, `ingressShim.*`, `dns01RecursiveNameservers*`, `prometheus.servicemonitor.*`, per-component `replicaCount`/`podDisruptionBudget`/`affinity`/`topologySpreadConstraints`, `cainjector.featureGates`); **feature-gate graduation/removal** (cluster enables `ServerSideApply`, `UseCertificateRequestBasicConstraints`, `ExperimentalGatewayAPISupport` on the controller and `ServerSideApply`, `CAInjectorMerging` on cainjector — a gate that GA's-and-is-removed or is dropped must be deleted from values); **CRD schema/apiVersion changes** (chart manages CRDs; verify the cluster's CRs still validate); minimum Kubernetes version bumps (cluster is on k8s v1.36); and any change to the CA/nameConstraints handling relevant to issue #7832 (fixed 1.17.4 — wrote permitted uriDomains into excludedSubtrees, broke istio-csr/ztunnel). Cross-check the chart `values.yaml` diff between from/to versions for renamed keys.
- **Skip-allowed**: **to be determined by subagent research.** Consult the cert-manager upgrade docs (https://cert-manager.io/docs/installation/upgrading/) for whether minor versions may be skipped or must be traversed sequentially, and record the authoritative constraint. Land on the latest patch of the chosen target minor.

### cert-manager-approver-policy

- **Identifies**: Helm chart `cert-manager-approver-policy` from `https://charts.jetstack.io` (HelmRepository `jetstack-charts`); image `quay.io/jetstack/cert-manager-approver-policy` (selected by chart `appVersion`). Provides `CertificateRequestPolicy` (`policy.cert-manager.io`) and gates cert-manager's CertificateRequest approvals — the cert-manager HR sets `disableAutoApproval: true` so approver-policy is load-bearing. Pre-1.0 (currently 0.16.0).
- **Stable definition**: a tag `vX.Y.Z` published as a non-prerelease GitHub release at https://github.com/cert-manager/approver-policy/releases AND a non-prerelease chart version at https://charts.jetstack.io/index.yaml. Reject anything with `-alpha`, `-beta`, `-rc` suffixes. Pre-1.0, so each `0.Y.0` minor is effectively a major per semver — treat minor bumps as potentially breaking.
- **Release notes**: per-release at https://github.com/cert-manager/approver-policy/releases/tag/v{version} ; project docs at https://cert-manager.io/docs/policy/approval/approver-policy/ ; README at https://github.com/cert-manager/approver-policy.
- **Breaking-change indicators**: each GitHub release notes page has "Breaking changes" / "Notable changes" subsections. Cross-check the `Chart.yaml` `appVersion` and the chart's `values.yaml` diff between from/to tags for renamed/removed values keys. Verify the approver-policy version's required **cert-manager minimum version** (the README/compatibility notes state it) against the planned cert-manager target — the two move together. Watch for `CertificateRequestPolicy` CRD schema changes (cluster ships RBAC for these in `app/certificate-request-policy-rbac.yaml`).
- **Skip-allowed**: **to be determined by subagent research.** Consult the approver-policy releases/README for whether pre-1.0 minor versions may be skipped or must be traversed sequentially, and record the authoritative constraint. Note the cert-manager↔approver-policy version coupling: the chosen approver-policy target must support the chosen cert-manager target. Land on the latest patch of the chosen target minor.

### kiali

- **Identifies**: Helm chart `kiali-server` from `https://kiali.org/helm-charts` (HelmRepository `kiali-charts`); image `quay.io/kiali/kiali` selected by chart `appVersion`. Deployed in `istio-system` as the Istio observability console.
- **Stable definition**: a tag `vX.Y.Z` published as a non-prerelease GitHub release at https://github.com/kiali/kiali/releases AND a matching non-prerelease `kiali-server` chart version in https://kiali.org/helm-charts/index.yaml. Reject `-snapshot`, `-rc`, `-alpha`, `-beta`. The latest stable is the highest such version **whose compatibility matrix lists the target Istio minor as supported** — Kiali is version-coupled to Istio.
- **Release notes**: per-tag GitHub release at https://github.com/kiali/kiali/releases/tag/v{version}; version compatibility matrix at https://kiali.io/docs/installation/installation-guide/prerequisites/ (Kiali ↔ Istio support table); chart values at https://github.com/kiali/helm-charts.
- **Breaking-change indicators**: GitHub release notes "Breaking Changes"/"Notable" sections; the Kiali↔Istio compatibility matrix (a Kiali release supports a bounded window of Istio minors); chart `values.yaml` diffs for renamed/removed keys (e.g. `external_services`, `deployment`, auth strategy keys); changes to required Istio telemetry/Prometheus config.
- **Skip-allowed**: yes — Kiali is a stateless console, so versions can be skipped. The hard constraint is landing on a Kiali version whose compatibility matrix supports the target Istio minor; bump Kiali together with or immediately after the Istio control-plane bump.

### fluent-operator

- **Identifies**: Helm chart `fluent-operator` from HelmRepository `fluent-charts` (https://fluent.github.io/helm-charts). CRDs: as of operator 3.8.0 the consolidated `manifests/setup/fluent-operator-crd.yaml` was removed and CRDs ship as two standalone published charts — `fluent-operator-fluent-bit-crds` and `fluent-operator-fluentd-crds` — whose CRDs live in `templates/` (so Helm install **and upgrade** them; prefer deploying these as their own HelmReleases over a remote-kustomize pull, which only ever installs). The data-plane images `ghcr.io/fluent/fluent-operator/fluent-bit:v{X.Y.Z}` (Fluent Bit upstream version) and `ghcr.io/fluent/fluent-operator/fluentd:v{X.Y.Z}` (Fluentd upstream version; the `-{n}` build-revision suffix was dropped after v1.17.x) are set on the `FluentBit` / `Fluentd` CRs, NOT via chart values. The image tags track the *upstream* Fluent Bit / Fluentd version streams (independently versioned from the operator).
- **Stable definition**: fluent-operator — an **appVersion** `X.Y.Z` published as a non-prerelease GitHub release at https://github.com/fluent/fluent-operator/releases; latest stable = highest such. ⚠️ **Chart version ≠ appVersion since chart 3.5.0.** The published Helm `chart` version diverged from the operator `appVersion`: chart `4.2.0`→app `3.9.0`, chart `4.1.0`→app `3.8.0`, chart `4.0.0`→app `3.7.0`, chart `3.5.0`→app `3.5.0` (last aligned). Look up the chart version for the target appVersion in https://fluent.github.io/helm-charts/index.yaml (`helm search repo fluent-charts/fluent-operator --versions`). The HelmRelease `chart.spec.version` takes the **chart** number; the CRD charts (`fluent-operator-fluent-bit-crds` / `fluent-operator-fluentd-crds`) carry the **same chart number** as the operator chart for a given release — keep all three on the same chart version. fluent-bit image — latest stable `v5.Y.Z` (v4.x is EOL) from https://github.com/fluent/fluent-bit/releases that the target operator release recommends/supports. fluentd image — the `ghcr.io/fluent/fluent-operator/fluentd` tag (`vX.Y.Z`, no suffix) the target operator release ships (verify the exact tag exists in GHCR).
- **Release notes**: fluent-operator per-tag at https://github.com/fluent/fluent-operator/releases/tag/v{version}; Fluent Bit at https://github.com/fluent/fluent-bit/releases and https://docs.fluentbit.io/manual/about/changelog; Fluentd at https://github.com/fluent/fluentd/releases.
- **Breaking-change indicators**: fluent-operator release notes "Breaking change"/"⚠️"/"Action required" callouts; the CRD diff between tags (new/changed CRD fields, removed APIs — CRDs and operator are deployed as separate releases so a CRD/operator version mismatch is a real failure mode); the chart `values.yaml` diff for renamed keys (e.g. `containerRuntime`, `fluentbit.crdsEnable`, `Kubernetes`, the `operator.*` sub-tree) — newer charts moved many hand-patched Deployment fields (env configMap/`CONTAINER_ROOT_DIR`, resources, probes, ports, service) into native `operator.*` values, so re-check post-renderers against the chart on every bump; for fluent-bit, watch the changelog for removed/renamed plugin config keys, default-behavior changes (e.g. multiline, storage/buffering, the v5.0.4 `retain_metadata_in_forward_mode` default flip that breaks Fluentd forward), and known regressions (memory leaks, RWX/executable-stack hardening); for fluentd, base-image/gem updates.
- **Skip-allowed**: yes — fluent-operator reconciles declarative CRs and the data plane is stateless (fluent-bit position DB and fluentd PVC buffer are runtime state, not schema), so operator minors can be skipped provided the CRD charts are upgraded to the same chart version as the operator in the same change. The hard constraint is CRD↔operator version equality, not sequential traversal. fluent-bit / fluentd image versions can jump directly to the operator-recommended tag. Policy recorded 2026-06-23: user wants the **most aggressive path the dep actually supports** — research the newest fluent-operator stable and jump straight there unless release notes document a required intermediate stop.

### grafana-operator

- **Identifies**: Helm chart `grafana-operator` from HelmRepository `grafana-charts` (`oci://ghcr.io/grafana/helm-charts` / grafana chart repo); operator image `ghcr.io/grafana/grafana-operator` (selected by chart `appVersion`); CRD manifest pinned separately as `https://github.com/grafana/grafana-operator/releases/download/v{version}/crds.yaml` in `cluster/gitops/monitoring/grafana/crds/kustomization.yaml`. The chart version and the CRD release URL must always be bumped to the same *release*, but **not the same string**: the chart repo dropped the `v` prefix from chart versions starting at **5.21.1** (grafana/grafana-operator#2377), while GitHub tags keep it. So for a target release `X.Y.Z` at or above 5.21.1, `spec.chart.spec.version` must be `X.Y.Z` (no `v`) and the CRD URL must be `.../releases/download/vX.Y.Z/crds.yaml` (with `v`). Below 5.21.1 both carry the `v`. Setting `vX.Y.Z` as the chart version at or above 5.21.1 fails chart resolution — no such chart version exists. This cluster sets `patches.flux.home.arpa/helmrelease.skip-crds: "true"` so the chart does NOT install CRDs; keep the chart value `crds.immutable` at its default `true`, since setting it `false` moves CRDs from the Helm `crds/` dir into `templates/` where `--skip-crds` no longer suppresses them, making the chart fight the separately-pinned kustomization for ownership.
- **Stable definition**: a tag `vX.Y.Z` published as a non-prerelease GitHub release at https://github.com/grafana/grafana-operator/releases AND present as a non-prerelease chart version in the grafana chart repo index. Reject anything with `-rc`, `-beta`, `-alpha` suffixes. Latest stable is the highest such tag.
- **Release notes**: per-release at https://github.com/grafana/grafana-operator/releases/tag/v{version} ; docs at https://grafana.github.io/grafana-operator/docs/ ; CRD API reference at https://grafana.github.io/grafana-operator/docs/api/
- **Breaking-change indicators**: GitHub release pages carry "Breaking changes" / "⚠️" callouts. Because CRDs are pinned independently here, always diff the `crds.yaml` between from/to versions for removed/renamed fields and any new required fields — the cluster has ~41 files defining `grafana.integreatly.org` CRs (GrafanaDashboard, GrafanaDatasource, GrafanaFolder, Grafana) across nearly every component, so a schema change has broad blast radius. Also check whether the operator's **default Grafana image** changed (the `Grafana` CR in this cluster does not pin a grafana image, so the app version is induced by the operator upgrade) and whether Grafana itself has breaking changes across that induced range.
- **Skip-allowed**: **Yes, minors may be skipped.** The operator is a stateless controller with declarative CRDs and no database migrations; a direct jump to the latest stable is supported, provided the CRD schema diff across the full skipped range is reviewed. Patch versions may always be skipped.
