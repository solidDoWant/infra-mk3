---
argument_specs:
  main:
    options:
      iso_name:
        type: str
        required: true
      dst_directory:
        type: str
        default: /var/lib/vz/template/iso/
      download_url:
        type: str
        required: true
